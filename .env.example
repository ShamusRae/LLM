# LLM Chat Application Environment Variables
# Copy this file to .env and adjust values as needed

# Service Ports (will be auto-discovered if not set)
BACKEND_PORT=3001
WRAPPER_PORT=3002
FRONTEND_PORT=5173

# Backend (for Mac/Linux/Azure)
# PUBLIC_HOST: used for callbacks and asset URLs (e.g. https://api.example.com)
# CORS_ORIGINS: comma-separated allowed origins (e.g. https://app.example.com,https://www.example.com)
# PUBLIC_HOST=
# CORS_ORIGINS=

# MCP Bridge
# internal (default) = existing in-process mcpService
# mcporter = external mcporter command adapter
# MCP_BRIDGE_PROVIDER=internal
# MCP_BRIDGE_SERVER=
# MCP_BRIDGE_STRICT=false
# MCP_BRIDGE_CONFIG_PATH=/absolute/path/to/mcp-bridge.json
# MCPORTER_COMMAND=npx
# MCPORTER_ARGS=mcporter
# MCP_BRIDGE_PATH_PREFIX=/opt/homebrew/bin:/usr/local/bin

# Frontend (Vite) - optional; used when frontend is on a different host/port
# VITE_BACKEND_URL=http://localhost:3001
# VITE_BACKEND_WS_ORIGIN=ws://localhost:3001
# VITE_AVATAR_WRAPPER_URL=http://localhost:3002
# Feature flags: VITE_ENABLE_PREVIEW_MODE, VITE_ENABLE_ADVANCED_SETTINGS, VITE_ENABLE_ANALYTICS, VITE_ENABLE_AUTO_SAVE, VITE_ENABLE_STEP_VALIDATION

# API Keys (optional - add your own)
OPENAI_API_KEY=your_openai_api_key_here
CLAUDE_API_KEY=your_claude_api_key_here
ANTHROPIC_API_KEY=your_anthropic_api_key_here
GOOGLE_API_KEY=your_google_api_key_here
# Optional alias:
# GEMINI_API_KEY=your_google_api_key_here

# Environment
NODE_ENV=development
